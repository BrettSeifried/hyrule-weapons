import React from 'react';

export default function ListCard({ items }) {
  return (
      <div>
          items.map((item) => {
              return (
                  <h3></h1>
              )
          })
      </div>
  )
}
